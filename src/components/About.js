function AboutSection() {
    return (
        <section id={"about"} className={"min-h-[calc(100vh-160px)]"}>
            <h1 className={"sectionHeader"}>
                <span className={"text-[#00ffff] mr-3"}>01.</span>About Me
            </h1>
        </section>
    );
}

export default AboutSection;
