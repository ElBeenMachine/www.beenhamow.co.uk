function Footer() {
    return (
        <footer className={"h-60 flex justify-center items-center"}>
            <img className={"h-20 py-5 w-auto"} src={"/img/logo.svg"} alt={"logo"} />
        </footer>
    );
}

export default Footer;
